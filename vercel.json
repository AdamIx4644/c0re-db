{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/main.py" }
  ],
  "build": {
    "env": {
      "MY_ENV_VAR": "my-value"
    },
    "install": "npm install && npm install --save-dev serverless-wsgi"
  }
}
